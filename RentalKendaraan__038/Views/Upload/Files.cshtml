@model RentalKendaraan__038.Models.Upload.FilesViewModel

<p>List of Files</p>

<ul>
    @foreach (var item in Model.Files)
    {
        <li>
            <a asp-action="Donwload"
                asp-route-filename="@item.Name">
                @item.Name
            </a>
        </li>
    }
</ul>